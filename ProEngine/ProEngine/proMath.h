#pragma once
#include "proPrerequisitesUtilities.h"

namespace proEngine {
  namespace Math {
    template <typename T>
     float sqrt(T n);
    int factorial(int n);
    template <typename T>
     float pow(T base, T exp);
    template <typename T>
     float cos(T n);
  }
}